// Metadata for login page
export const metadata = {
  title: "Login | ShopNext",
  description: "Login to access admin panel and manage products.",
};

export default function LoginLayout({ children }) {
  return children;
}
