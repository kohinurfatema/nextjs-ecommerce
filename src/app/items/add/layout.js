// Metadata for Add Item page
export const metadata = {
  title: "Add Product | ShopNext",
  description: "Add a new product to your store inventory.",
};

export default function AddItemLayout({ children }) {
  return children;
}
